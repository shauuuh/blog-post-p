import { useState, useEffect } from react;

function register(props) {
  return (
    <form action="" >
      <input 
        type="text"
        placeholder="Username"
        value={username}
        onChange
      />
      <input 
        type="password"
        placeholder="Password"
        value={password}
      />
      <button />
    </form>
  );
}